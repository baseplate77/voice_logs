import 'package:flutter/material.dart';

/// Settings — storage usage, Gemma download status, export/delete, thermal
/// preferences.
///
/// Phase 0 placeholder.
class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Settings (Phase 6)')));
  }
}

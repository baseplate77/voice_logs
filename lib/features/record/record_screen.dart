import 'package:flutter/material.dart';

/// Recording screen — big record button, live waveform, live caption.
///
/// Phase 0 placeholder; all behavior arrives in Phase 1.
class RecordScreen extends StatelessWidget {
  const RecordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Record (Phase 1)')));
  }
}

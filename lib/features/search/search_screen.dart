import 'package:flutter/material.dart';

/// Hybrid search screen — FTS5 + vector + entity-boosted results, merged via
/// reciprocal rank fusion.
///
/// Phase 0 placeholder; implementation lands in Phases 2–3.
class SearchScreen extends StatelessWidget {
  const SearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Search (Phase 2+)')));
  }
}
